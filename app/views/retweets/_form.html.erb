<%= form_with(model: retweet) do |form| %>
  <% if retweet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(retweet.errors.count, "error") %> prohibited this retweet from being saved:</h2>

      <ul>
        <% retweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quote_tweet, style: "display: block" %>
    <%= form.number_field :quote_tweet %>
  </div>

  <div>
    <%= form.label :retweeted, style: "display: block" %>
    <%= form.number_field :retweeted %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
