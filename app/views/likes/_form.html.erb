<%= form_with(model: like) do |form| %>
  <% if like.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
        <% like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :handle, style: "display: block" %>
    <%= form.text_field :handle, value: params["handle"] %>
  </div>

  <div>
    <%= form.label :tweet_id, style: "display: block" %>
    <%= form.text_field :tweet_id, value: params["tweet_id"] %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
